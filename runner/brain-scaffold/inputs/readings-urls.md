# Readings — URLs

*URLs to deliver as readings (one per session, from the readings pool).*
*Format: `- https://example.com/essay  <!-- brief description -->`*
*After delivery: runner marks with `✔ read: N`*
